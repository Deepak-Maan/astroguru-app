name: Auto OTA Update on Git Push

on:
  push:
    branches:
      - main

jobs:
  publish-ota-update:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Checkout Repository
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js Environment
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: ⚡ Setup Expo and EAS CLI
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 📦 Install Dependencies
        run: npm install

      - name: 🚀 Publish Expo OTA Update to All Channels (Preview, Production, Main)
        run: |
          eas update --channel production --message "Auto OTA Update (Production) [${{ github.sha }}]" --non-interactive || true
          eas update --channel preview --message "Auto OTA Update (Preview) [${{ github.sha }}]" --non-interactive || true
          eas update --branch main --message "Auto OTA Update (Main) [${{ github.sha }}]" --non-interactive || true
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

      - name: 🌐 Build Production Web Bundle
        run: npm run build:web
